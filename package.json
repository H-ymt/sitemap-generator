{
  "name": "sitemap-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd workers && wrangler dev --port 3000 --local",
    "build": "cd workers && pnpm install && pnpm build",
    "deploy": "cd workers && wrangler deploy",
    "workers:build": "cd workers && pnpm install && pnpm build",
    "workers:deploy": "cd workers && wrangler deploy",
    "workers:dev": "cd workers && wrangler dev --port 3000 --local",
    "tail": "cd workers && wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5"
  }
}
